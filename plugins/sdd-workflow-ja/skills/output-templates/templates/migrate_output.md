## AI-SDD マイグレーション

### 検出された構成

| 項目         | 現在の値                  | 新しい推奨値        |
|:-----------|:----------------------|:--------------|
| ルートディレクトリ  | `.docs`               | `.sdd`        |
| 要求仕様ディレクトリ | `requirement-diagram` | `requirement` |
| タスクディレクトリ  | `review`              | `task`        |

### 移行オプション

**A: 新構成に移行**

- ディレクトリをリネームして新しい命名規則に統一
- 推奨: 新規プロジェクトや初期段階のプロジェクト

**B: 旧構成を維持**

- `.sdd-config.json` を生成して現在の構成を維持
- 推奨: 多くの外部参照がある既存プロジェクト

どちらのオプションを選択しますか？

---

## マイグレーション完了

### 実行された変更

- [x] `.docs/` → `.sdd/` にリネーム
- [x] `requirement-diagram/` → `requirement/` にリネーム
- [x] `review/` → `task/` にリネーム
- [x] CLAUDE.md のパス参照を更新

### 次のステップ

1. 既存のスクリプトや参照を確認し、必要に応じて更新
2. `git status` で変更を確認

### 推奨する手動検証

- [ ] `git status` で変更内容を確認
- [ ] 既存のスクリプトや CI/CD パイプラインがディレクトリパスを参照している場合は更新
- [ ] CLAUDE.md 内のパス参照が正しく更新されていることを確認
- [ ] 他のドキュメント内のリンクが正しいことを確認

### 検証コマンド

```bash
# Git の変更状態を確認
git status

# ディレクトリ構造を確認
ls -la .sdd/

# AI-SDD コマンドが正常に動作することを確認
/check_spec
```

---

## マイグレーション完了

### 生成されたファイル

- [x] `.sdd-config.json` を作成

### 設定内容

```json
{
  "root": ".docs",
  "directories": {
    "requirement": "requirement-diagram",
    "specification": "specification",
    "task": "review"
  }
}
```

### 次のステップ

1. `.sdd-config.json` をバージョン管理に追加

---

⚠️ このプロジェクトはGit管理下にありません。

オプションA（ディレクトリリネーム）を選択する場合、
手動でのバックアップを推奨します。

続行しますか？

---

✅ このプロジェクトは既に新しい構成を使用しています。

マイグレーションは不要です。

現在の構成:

- ルートディレクトリ: .sdd
- 要求仕様: requirement
- タスク: task

---

ℹ️ `.sdd-config.json` が既に存在します。

現在の設定:

- root: {現在の値}
- directories.requirement: {現在の値}
- directories.task: {現在の値}

この設定に基づいてプラグインは動作します。
設定を変更する場合は、手動で `.sdd-config.json` を編集してください。
